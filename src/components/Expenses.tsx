import React, { FC } from "react";
import { Expense } from "../interfaces/Models";

interface IExpenseList{
    total: number
}

const ExpenseList : FC<IExpenseList> = ({total}) => {

    


    return(
        <>
        </>
    );
}

const Expense : FC<Expense> = () => {


    return(
        <>
        </>
    );
} 