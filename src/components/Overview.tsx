import React, { FC } from "react";

interface IOverview{
    total : number
}

const Overview : FC<IOverview> = ({
    total,
}) => {
    return(
        <>

        </>
    )
} 

export default Overview;